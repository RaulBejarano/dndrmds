<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_green_button" >

    <ImageView
        android:id="@+id/row_attack_image"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_margin="10dp"
        android:src="@drawable/icon_empty" />

    <TextView
        android:id="@+id/row_attack_name"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignBottom="@+id/row_attack_image"
        android:layout_alignTop="@+id/row_attack_image"
        android:layout_toLeftOf="@+id/row_attack_level"
        android:layout_toRightOf="@+id/row_attack_image"
        android:gravity="center"
        android:paddingLeft="10dp"
        android:text="Medium Text"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <TextView
        android:id="@+id/row_attack_level"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignBottom="@+id/row_attack_name"
        android:layout_alignParentRight="true"
        android:background="@drawable/background_level"
        android:gravity="center"
        android:text="0"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="#FFFFFF"
        android:textSize="16sp" />

</RelativeLayout>
